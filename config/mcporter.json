{
  "mcpServers": {
    "vercel-domains": {
      "command": "npx",
      "args": ["-y", "vercel-domains-mcp"],
      "env": {
        "VERCEL_ACCESS_TOKEN": "${VERCEL_ACCESS_TOKEN}"
      }
    },
    "firecrawl": {
      "description": "Firecrawl hosted MCP for authenticated scraping and extraction.",
      "baseUrl": "https://mcp.firecrawl.dev/v2/mcp",
      "headers": {
        "Authorization": "Bearer ${FIRECRAWL_API_KEY}"
      }
    },
    "chrome-devtools": {
      "description": "Attach chrome-devtools-mcp to the live Oracle browser session.",
      "command": "npx",
      "args": ["-y", "chrome-devtools-mcp@latest", "--browserUrl", "${CHROME_DEVTOOLS_URL}"],
      "lifecycle": "ephemeral"
    },
    "iterm": {
      "command": "/Users/steipete/Projects/iterm-mcp/build/index.js"
    },
    "oracle": {
      "command": "npx",
      "args": ["--yes", "--package", "@zeta987/oracle", "oracle-mcp"],
      "root": ".."
    },
    "oracle-local": {
      "description": "Local oracle-mcp built from dist/bin/oracle-mcp.js",
      "command": "node",
      "args": ["../dist/bin/oracle-mcp.js"],
      "root": ".."
    }
  },
  "imports": []
}
